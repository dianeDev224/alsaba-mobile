import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    dateContent: {
        borderWidth: 3,
        borderRadius: 10,
        width: 200,
        height: 40,
        textAlign: 'center',
        padding: 10,
        marginLeft: 1

    }

})