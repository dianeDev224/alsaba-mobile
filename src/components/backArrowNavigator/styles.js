import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        padding: 5,
        position: 'absolute',
        top: 2,
        left: 2
    },
    boldText: {
        fontWeight: 'bold',
        fontSize: 16
    },
})